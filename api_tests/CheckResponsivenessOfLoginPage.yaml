# ********RoostGPT********

# Test generated by RoostGPT for test lseg-integration using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# ROOST_METHOD_HASH=09592aa030
# ROOST_METHOD_SIG_HASH=af038db8f3
# 
#    ########## Scenario ##########  
# 
# {
#   background: null,
#   rule: null,
#   scenario: 'Check the responsiveness of the login page\n' +
#     'Given the frontend style sheets of the application\n' +
#     'When I test the CSS using different screen sizes\n' +
#     'Then The component styling should adjust according to screen size',
#   title: 'Check the responsiveness of the login page'
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "CheckResponsivenessOfLoginPage.csv"
    fields: 
      - "email"
      - "password"  

scenarios:

# Scenario 1
  - name: "Check the responsiveness of the login page"
    flow:
      - get:
          url: "/login"
          headers: {}
          expect:
            - statusCode: 200
            - hasProperty: "css"
      - log: "Initial page load was successful"
      - think: 1

# Scenario 2
  - name: "Login Functionality with different user credentials"
    flow:
      - post:
          url: "/login"
          headers: {}
          json:
            email: "{{ email }}"
            password: "{{ password }}"
          expect:
            - statusCode: 200
            - hasProperty: "user"
            - hasProperty: "status"
            - equals: 
                status: "success"
      - log: "Login was successful with provided user credentials"
      - think: 1
